const numeros = [10, 20, 30, 40, 50];

for (let indice = 0; indice < numeros.length; indice++) {
    console.log(numeros[indice]);
}

// Exemplo : Imprima apenas os números ímpares
console.log("Imprimindo apenas os ímpares: ");
for (let indice = 0; indice < numeros.length; indice++) {
    if (numero[indice] % 2 === 1) {
        console.log(numeros[indice]);
    }
}