const produto = {
    nome: "Smartphone",
    preco: 1500,
    modelo: "A01",
};

// Keys()
let keys = Object.keys(produto);
console.log(keys);

// values()
let values = Object.values(produto);
console.log(values);